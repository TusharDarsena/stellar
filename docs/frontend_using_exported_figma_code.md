Smart constraint. Let me think about how to compress 15 prompts into 5 without losing anything critical.

The grouping logic: **structure + types in one shot**, **shared components in one shot**, **attendee flow in one shot**, **organizer + scanner in one shot**, **wiring in one shot**.

---

**Prompt 1 — Project foundation, design system, types, and app shell (~580 words)**

> Create a new Vite + React + TypeScript project with Tailwind CSS v4. Create `src/styles/globals.css` with these exact CSS custom properties and map them to Tailwind utilities via `@theme inline`:
>
> ```css
> :root {
>   --background: #0E1113; --foreground: #EAEFF4;
>   --card: #15181C; --card-foreground: #EAEFF4;
>   --primary: #7C5CFF; --primary-foreground: #EAEFF4;
>   --secondary: #272C33; --secondary-foreground: #B7C0CC;
>   --muted: #272C33; --muted-foreground: #B7C0CC;
>   --border: #272C33; --input: #15181C; --ring: #7C5CFF;
>   --destructive: #EF4444; --success: #22C55E; --warning: #F59E0B;
>   --radius: 0.625rem;
> }
> ```
>
> Apply `bg-background text-foreground` to body. Install shadcn/ui, lucide-react. Install `qrcode.react` package.
>
> Create `src/types/index.ts` with these exact types:
> ```typescript
> export type TicketStatus = 'Active' | 'Used' | 'Refunded';
> export type EventStatus = 'Active' | 'Cancelled' | 'Completed';
> export type WalletType = 'web3auth' | 'freighter';
> export type TxStatus = 'idle' | 'building' | 'signing' | 'submitting' | 'success' | 'error';
> export type AppView = 'landing' | 'browse' | 'event-detail' | 'purchase' | 'my-tickets' | 'qr-display' | 'organizer-dashboard' | 'organizer-create' | 'scanner';
> export interface Event { eventId: string; organizer: string; name: string; dateUnix: number; capacity: number; pricePerTicket: number; currentSupply: number; status: EventStatus; imageUrl?: string; description?: string; venue?: string; city?: string; }
> export interface Ticket { ticketId: string; eventId: string; owner: string; status: TicketStatus; }
> export interface WalletState { isConnected: boolean; publicKey: string | null; walletType: WalletType | null; xlmBalance: string | null; displayName?: string; email?: string; avatarUrl?: string; }
> export interface TxState { status: TxStatus; hash?: string; errorMessage?: string; }
> export const stroopsToXlm = (s: number) => (s / 10_000_000).toFixed(2);
> export const formatEventDate = (unix: number) => new Date(unix * 1000).toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric', year: 'numeric' });
> export const truncateKey = (k: string) => `${k.slice(0,4)}...${k.slice(-4)}`;
> ```
>
> Create `src/App.tsx` with `useState` for `currentView: AppView` (initial `'landing'`), `selectedEventId: string | null`, `selectedTicketId: string | null`, `walletState: WalletState` (initial all nulls, `isConnected: false`), `txState: TxState` (initial `{status: 'idle'}`). Define 5 mock events with realistic values (`pricePerTicket` in stroops e.g. `15000000`, future `dateUnix`, populated `imageUrl` using Unsplash URLs, `venue`, `city`, `description`, `status: 'Active'`). Define 2 mock tickets (one `Active`, one `Used`). Render `<div className="min-h-screen bg-background text-foreground">TODO</div>`.
>
> Create `src/components/ui/` with these components, all using CSS var tokens not hardcoded hex: `Button` (variants: primary/secondary/outline/ghost/destructive, sizes: sm/md/lg/icon, `loading` boolean prop showing spinning `Loader2`), `Badge` (variants: default/success/warning/destructive/primary — all using /10 background with colored border and text), `Card` + `CardHeader` + `CardContent` + `CardFooter` (`bg-card border border-border rounded-xl`), `Input` (`bg-input border border-border rounded-lg focus:ring-2 focus:ring-ring`), `TxOverlay` (takes `txState: TxState` prop — renders nothing when idle; fixed inset-0 `bg-black/80 z-50` centered card when building/signing/submitting showing spinning `Loader2`, correct status message "Building..." / "Waiting for signature..." / "Submitting to Stellar...", and "Do not close this page"; on success shows green checkmark + "Confirmed" + hash in monospace; on error shows red X + errorMessage + dismiss button).
>
> Create `src/components/layout/AppHeader.tsx` — sticky, `bg-card border-b border-border h-16`. Left: `Ticket` icon (text-primary) + "StellarTickets" bold. Center desktop only: search input. Right: when not connected shows "Connect Wallet" primary button that on click shows a small dropdown with "Attendee (Web3Auth)" and "Organizer (Freighter)" options; when connected shows a `bg-secondary rounded-full px-3 py-2` pill with green dot + truncated public key + XLM balance + chevron, clicking shows dropdown with copy address and disconnect. Props: `walletState`, `onConnect(type: WalletType)`, `onDisconnect`, `onNavigate(view: AppView)`.
>
> Create `src/components/layout/BottomNav.tsx` — `fixed bottom-0 left-0 right-0 bg-card border-t border-border md:hidden`. Always shows: Home (`House` → browse), Tickets (`Ticket` → my-tickets, badge with count). Freighter wallet adds: Dashboard (`LayoutDashboard` → organizer-dashboard), Scanner (`ScanLine` → scanner). Web3Auth adds: Saved (`Heart`). Active item text-primary, inactive text-muted-foreground. Props: `currentView`, `onNavigate`, `ticketCount`, `walletType`.

---

**Prompt 2 — Shared event components and Browse + Event Detail pages (~600 words)**

> Create `src/components/events/EventCard.tsx`. Props: `event: Event`, `onClick: () => void`. Mobile-first card `bg-card border border-border rounded-xl overflow-hidden cursor-pointer hover:border-primary/30 transition-all group w-full`. Image area `aspect-video relative overflow-hidden bg-secondary`: if `imageUrl` show `img object-cover w-full h-full group-hover:scale-105 transition-transform duration-300`, else show centered `Music` icon in `text-muted-foreground`. Overlays on image: top-right `bg-black/50 backdrop-blur-sm rounded-full px-2 py-1 text-xs text-white` showing `{capacity - currentSupply} left`; top-left status Badge only if status is not Active. Below image `p-4`: event name `font-semibold text-base line-clamp-1`; `MapPin h-3 w-3 inline` + venue + city in `text-muted-foreground text-sm mt-1 line-clamp-1`; `Calendar h-3 w-3 inline` + `formatEventDate(dateUnix)` in `text-muted-foreground text-sm`; bottom row: `font-bold text-primary text-lg` showing `{stroopsToXlm(pricePerTicket)} XLM` and a "Get Tickets" Button variant primary size sm.
>
> Create `src/pages/BrowsePage.tsx`. Props: `events: Event[]`, `onEventClick: (id: string) => void`, `walletState: WalletState`. Full layout with `AppHeader` at top. Below header `container mx-auto px-4 py-6 pb-24 md:pb-8`. Horizontal scrollable category filter bar (no scrollbar shown) with pill buttons: All / Music / Sports / Theater / Comedy / Festivals. Local `useState` for active category. Active pill `bg-primary text-primary-foreground rounded-full px-4 py-1.5 text-sm font-medium`, inactive `bg-secondary text-secondary-foreground`. Events grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-5`. Empty state: centered `Frown` icon + "No events found" + clear filters button. Each grid item renders `EventCard` calling `onEventClick(event.eventId)`.
>
> Create `src/pages/EventDetailPage.tsx`. Props: `event: Event`, `onBack: () => void`, `onBuyTickets: () => void`, `walletState: WalletState`. Sticky header `bg-card border-b border-border` with `ChevronLeft` back button + event name truncated + `Share2` icon. Below header: full-width `aspect-video` hero image or dark placeholder. Then `container mx-auto px-4 py-6 pb-24 md:pb-8`. On desktop `lg:grid lg:grid-cols-3 gap-8`, mobile single column. Left col `lg:col-span-2`: `h1 text-2xl font-bold` event name; organizer address in `text-muted-foreground text-sm font-mono` with `Key h-3 w-3` icon; four info rows each with icon + label + value: `Calendar` date, `Clock` time derived from dateUnix, `MapPin` venue+city, `Users` `{currentSupply}/{capacity} sold`. Progress bar `bg-secondary rounded-full h-1.5 mt-1` with inner `bg-primary` at percentage. Description paragraph `text-muted-foreground mt-4 leading-relaxed`. Right col (sticky `top-20`): `bg-card border border-primary/20 rounded-2xl p-6`. Price `text-4xl font-bold text-primary` + `text-muted-foreground text-sm "per ticket"`. Tickets remaining text. Full-width Button primary lg "Buy Ticket" calling `onBuyTickets` — disabled+text "Sold Out" if `currentSupply >= capacity`, disabled+"Cancelled" if status Cancelled. Below button three trust badges in a row: `Shield` "On-chain", `Zap` "Instant", `RefreshCw` "Refundable" each in `text-muted-foreground text-xs flex items-center gap-1`.
>
> Create `src/pages/PurchasePage.tsx`. Props: `event: Event`, `onBack: () => void`, `onConfirm: (qty: number) => void`, `txState: TxState`, `walletState: WalletState`. Local state: `step: 'select' | 'confirm'`, `quantity: number` (initial 1). Sticky header back arrow + "Buy Tickets". Step 1: small event summary card (thumbnail + name + date); quantity selector centered — circular outline minus button, `text-5xl font-bold w-20 text-center` quantity, plus button (disabled at max available); `text-muted-foreground text-sm text-center mt-2` "{available} tickets available"; order summary card showing line item + divider + bold total in XLM; full-width primary lg "Continue" button advancing to step 2. Step 2: order summary read-only; payment card showing `Wallet` icon + "XLM from your Stellar wallet" + connected public key truncated (non-editable, purely informational); `text-muted-foreground text-xs` note "Held in escrow until after event. Refundable if cancelled."; full-width primary lg "Confirm & Pay {total} XLM" calling `onConfirm(quantity)`; ghost "Go Back" link. Render `TxOverlay` over page.

---

**Prompt 3 — Attendee ticket management: My Tickets, QR Display (~580 words)**

> Create `src/components/tickets/TicketCard.tsx`. Props: `ticket: Ticket`, `event: Event | undefined`, `onShowQR: () => void`, `onRefund: () => void`. Card `bg-card border border-border rounded-xl overflow-hidden flex`. Left accent bar `w-1.5 flex-shrink-0` colored `bg-primary` if Active, `bg-muted-foreground` if Used, `bg-destructive` if Refunded. Content `p-4 flex-1`. Event name `font-semibold` or "Unknown Event" if undefined. Date + venue `text-muted-foreground text-sm mt-0.5`. Ticket ID `font-mono text-xs text-muted-foreground/60 mt-1` (full string). Status Badge row `mt-2`. Actions row `mt-3 flex gap-2`: "Show QR" Button primary sm (only if Active), "Request Refund" Button outline sm (only if Active AND event?.status === 'Cancelled'). On mobile these buttons should be full-width stacked below content.
>
> Create `src/pages/MyTicketsPage.tsx`. Props: `tickets: Ticket[]`, `events: Event[]`, `onShowQR: (ticketId: string) => void`, `onRefund: (ticketId: string) => void`, `txState: TxState`, `walletState: WalletState`. Layout: AppHeader at top. `container mx-auto px-4 py-6 pb-24 md:pb-8`. `h1 text-2xl font-bold` "My Tickets" + count badge. Tab bar with two pills "Active" and "History" using local `useState<'active' | 'history'>`. Active tab filters `status === 'Active'`, History shows Used and Refunded. List `flex flex-col gap-3 mt-5` of TicketCard components. Empty state for each tab: Active empty — `Ticket` icon, "No active tickets", "Browse events to buy tickets", Button primary "Browse Events"; History empty — `Clock` icon, "No ticket history". Render TxOverlay.
>
> Create `src/pages/QRDisplayPage.tsx`. Props: `ticket: Ticket`, `event: Event | undefined`, `walletState: WalletState`, `onBack: () => void`. This page is `fixed inset-0 bg-black z-40 flex flex-col` — intentionally chromeless, no AppHeader, no BottomNav.
>
> If `ticket.status === 'Used'`: full screen `bg-red-950` centered flex-col, `XCircle h-24 w-24 text-destructive`, "Ticket Already Used" `text-2xl font-bold text-white mt-4`, "This ticket was scanned at the venue" `text-white/60 text-sm mt-2`, "Go Back" Button outline mt-6.
>
> If `ticket.status === 'Refunded'`: same layout, `bg-neutral-900`, `Ban` icon, "Ticket Refunded", "Your XLM was returned when the event was cancelled."
>
> If `ticket.status === 'Active'`: three flex zones filling the screen. Top zone `px-6 pt-10 pb-4 flex items-center relative`: `ChevronLeft h-6 w-6 text-white cursor-pointer` calling onBack absolute left; event name `text-white font-semibold text-center w-full`; date `text-white/60 text-sm text-center mt-1`. Middle zone `flex-1 flex flex-col items-center justify-center gap-6 px-6`: a `w-64 h-64 bg-white rounded-2xl p-4 flex items-center justify-center shadow-2xl shadow-primary/20` QR container — import `QRCode` from `qrcode.react` and render `<QRCode value={`${walletState.publicKey}:${ticket.ticketId}:${Math.floor(Date.now()/1000)}`} size={220} bgColor="#ffffff" fgColor="#0E1113" />`. Note in comment `// TODO: replace value with signed payload from lib/qr.ts`. Below QR container a countdown pill `bg-white/10 backdrop-blur-sm rounded-full px-4 py-2 flex items-center gap-2`: `RefreshCw h-3.5 w-3.5 text-white/50 animate-spin` + `text-white/70 font-mono text-sm "Refreshes in {countdown}s"`. Countdown via `useEffect` with `setInterval(1000)` counting 30→0 then resetting, and resetting the QR value (force re-render) each reset. Bottom zone `px-6 pb-12 flex flex-col items-center gap-1.5`: `Lock h-3.5 w-3.5 text-white/30` inline + "Signed with your wallet key" `text-white/30 text-xs`; wallet public key truncated `font-mono text-white/25 text-xs`.

---

**Prompt 4 — Organizer flow and Scanner page (~600 words)**

> Create `src/components/organizer/OrganizerEventRow.tsx`. Props: `event: Event`, `onReleaseFunds: () => void`. Card `bg-card border border-border rounded-xl p-4`. Mobile: stacked layout. Desktop: `flex items-center gap-4`. Left block flex-1: name `font-semibold`, date `text-muted-foreground text-sm`, venue `text-muted-foreground text-sm`. Center block: `{event.currentSupply} / {event.capacity} sold` `text-sm`; mini progress bar `bg-secondary rounded-full h-1 mt-1 w-32` with inner `bg-primary` at percentage; status Badge. Right block `flex flex-col items-end gap-2 flex-shrink-0`: escrow amount `font-bold text-primary text-sm` showing `{stroopsToXlm(event.currentSupply * event.pricePerTicket)} XLM` with label "in escrow" `text-muted-foreground text-xs`; Release Funds Button — disabled (cursor-not-allowed opacity-50) with tooltip-style `title` "Available after event date" if `Date.now()/1000 < event.dateUnix` OR `event.status !== 'Active'`; when enabled render as Button variant warning (use `bg-warning text-black` inline style) size sm with `ArrowUpFromLine h-4 w-4` calling `onReleaseFunds`.
>
> Create `src/pages/organizer/DashboardPage.tsx`. Props: `events: Event[]`, `organizerPublicKey: string`, `onCreateEvent: () => void`, `onBack: () => void`, `onReleaseFunds: (eventId: string) => void`, `txState: TxState`, `walletState: WalletState`. Filter events to `event.organizer === organizerPublicKey`. Header: `AppHeader` with back behavior. `container mx-auto px-4 py-6 pb-24 md:pb-8`. Top: `flex items-center justify-between mb-6` — "Dashboard" h1 + Button primary sm `Plus` icon "Create Event" calling onCreateEvent. Stats grid `grid grid-cols-3 gap-3 mb-6`: three `bg-card border border-border rounded-xl p-4 text-center` cards showing Events count, Tickets Sold sum, In Escrow XLM sum — each with `text-2xl font-bold` value and `text-muted-foreground text-xs` label. Events list `flex flex-col gap-3` of OrganizerEventRow. Empty state: `CalendarX h-16 w-16 text-muted-foreground mx-auto`, "No events yet", Button primary "Create Your First Event". Render TxOverlay.
>
> Create `src/pages/organizer/CreateEventPage.tsx`. Props: `onBack: () => void`, `onSubmit: (data: any) => void`, `txState: TxState`, `walletState: WalletState`. Single `useState` object for form: name, date (string), time (string), venue, city, capacity (number), priceXlm (number), imageUrl, description. Header back + "Create Event". `container mx-auto px-4 py-6 pb-24 md:pb-8`. Form fields `flex flex-col gap-4`: all using Input component — Event Name, Date `type="date"`, Time `type="time"`, Venue, City, Capacity `type="number" min=1`, Price per Ticket (XLM) `type="number" min=0.01 step=0.01` with comment `// × 10_000_000 = stroops on submit`, Cover Image URL, Description as `textarea` 4 rows. Below form a live preview card (read-only EventCard-like render updating as fields change, placeholder image if no URL). Submit: full-width primary lg Button "Create Event on Stellar" `loading={txState.status !== 'idle'}`. Below: `text-muted-foreground text-xs text-center` "Requires Freighter signature." Render TxOverlay.
>
> Create `src/pages/ScannerPage.tsx`. Props: `onBack: () => void`, `onMarkUsed: (ticketId: string) => void`, `txState: TxState`, `walletState: WalletState`. Local state: `scanResult: ScanResult | null`. `ScanResult` type defined locally: `{verdict: 'valid'; ticketId: string; eventName: string; walletAddress: string} | {verdict: 'invalid_signature'} | {verdict: 'expired'} | {verdict: 'already_used'}`. Layout: AppHeader back + "Scanner". `container mx-auto px-4 py-6 pb-24 md:pb-8 max-w-sm mx-auto`. Camera viewport `relative w-full aspect-square bg-black rounded-2xl overflow-hidden mb-4`: inner placeholder `flex items-center justify-center h-full` with `Camera h-12 w-12 text-muted-foreground` + "Camera appears here" text. Absolute overlay: four corner brackets using absolute-positioned divs `w-8 h-8` at each corner with two `border-white border-2` perpendicular edges (top+left, top+right, bottom+left, bottom+right). Below viewport: "Start Scanning" Button primary lg full-width. Demo row `flex gap-2 mt-4 justify-center`: three outline sm Buttons "✓ Valid" / "⏱ Expired" / "✗ Used" setting scanResult accordingly with mock data. When `scanResult !== null`: `fixed inset-0 z-50 flex flex-col items-center justify-center p-8` overlay. Valid: `bg-success` — `CheckCircle2 h-24 w-24 text-white`, "Entry Granted" `text-3xl font-bold text-white`, wallet truncated, event name, "Scan Next" white outline Button calling onMarkUsed then reset. Invalid/Expired/Used: `bg-destructive/95` — `XCircle h-24 w-24 text-white`, appropriate heading, reason subtext, "Try Again" Button resetting scanResult. Render TxOverlay.

---

**Prompt 5 — Wire App.tsx, landing page, complete navigation (~570 words)**

> Create `src/pages/LandingPage.tsx`. Props: `onSelectAttendee: () => void`, `onSelectOrganizer: () => void`. `min-h-screen bg-background flex flex-col items-center justify-center px-6`. Top: `Ticket h-12 w-12 text-primary`, `h1 text-4xl font-bold text-center mt-4` "StellarTickets", `p text-muted-foreground text-center mt-2` "NFT event tickets on the Stellar blockchain". Below `mt-12 w-full max-w-sm flex flex-col gap-4`: two cards each `bg-card border border-border rounded-2xl p-6 cursor-pointer hover:border-primary/50 transition-all active:scale-95 flex items-center gap-4`. Attendee card: `Users h-10 w-10 text-primary flex-shrink-0`; text block flex-1 — `h2 font-semibold text-lg` "I'm an Attendee", `p text-muted-foreground text-sm mt-0.5` "Browse events, buy tickets with XLM"; `ArrowRight h-5 w-5 text-muted-foreground`. Organizer card: `LayoutDashboard h-10 w-10 text-primary flex-shrink-0`; "I'm an Organizer", "Create events, manage sales, release funds". Bottom `mt-16 text-muted-foreground/40 text-xs text-center` "Powered by Stellar · Soroban smart contracts".
>
> Now fully replace `src/App.tsx`. Keep the existing state and mock data. Add a `mockWalletConnected: WalletState` constant: `{ isConnected: true, publicKey: 'GCEZDNQO6LFX5LFNMXJ7V2YPKD7DEMO', walletType: 'web3auth', xlmBalance: '5.25', displayName: 'Demo User', email: 'demo@example.com', avatarUrl: undefined }`. Add a `mockOrganizerWallet: WalletState` constant: same but `walletType: 'freighter'`, `displayName: undefined`, `email: undefined`.
>
> Callbacks: `handleConnect(type)` — if `'web3auth'` set walletState to mockWalletConnected; if `'freighter'` set to mockOrganizerWallet. `handleDisconnect` resets walletState to defaults. `handleEventClick(id)` sets selectedEventId and navigates to `'event-detail'`. `handleBuyTickets` navigates to `'purchase'`. `handleBack` navigates back one logical step based on currentView: event-detail→browse, purchase→event-detail, qr-display→my-tickets, organizer-create→organizer-dashboard, organizer-dashboard→landing, scanner→browse. `handleConfirmPurchase(qty)` sets `txState` through the full sequence: `building` → after 1s `signing` → after 1s `submitting` → after 1.5s `success` with a mock hash → after 3s resets to `idle` and navigates to `'my-tickets'`. `handleReleaseFunds(eventId)` same sequence pattern. `handleMarkUsed(ticketId)` same.
>
> `renderView()` switch: `landing` → `<LandingPage onSelectAttendee={() => { handleConnect('web3auth'); navigate('browse'); }} onSelectOrganizer={() => { handleConnect('freighter'); navigate('organizer-dashboard'); }} />`; `browse` → `<BrowsePage events={mockEvents} onEventClick={handleEventClick} walletState={walletState} />`; `event-detail` → lookup selectedEvent, render `<EventDetailPage event={selectedEvent} onBack={handleBack} onBuyTickets={handleBuyTickets} walletState={walletState} />`; `purchase` → `<PurchasePage event={selectedEvent} onBack={handleBack} onConfirm={handleConfirmPurchase} txState={txState} walletState={walletState} />`; `my-tickets` → `<MyTicketsPage tickets={mockTickets} events={mockEvents} onShowQR={(id) => { setSelectedTicketId(id); navigate('qr-display'); }} onRefund={() => {}} txState={txState} walletState={walletState} />`; `qr-display` → lookup selectedTicket and its event, render `<QRDisplayPage ticket={selectedTicket} event={selectedEvent} walletState={walletState} onBack={handleBack} />`; `organizer-dashboard` → `<DashboardPage events={mockEvents} organizerPublicKey={walletState.publicKey ?? ''} onCreateEvent={() => navigate('organizer-create')} onBack={handleBack} onReleaseFunds={handleReleaseFunds} txState={txState} walletState={walletState} />`; `organizer-create` → `<CreateEventPage onBack={handleBack} onSubmit={() => {}} txState={txState} walletState={walletState} />`; `scanner` → `<ScannerPage onBack={handleBack} onMarkUsed={handleMarkUsed} txState={txState} walletState={walletState} />`.
>
> Wrap renderView in a root div. Show `<AppHeader>` on all views except landing and qr-display. Show `<BottomNav>` on all views except landing and qr-display, hidden on md and above. Mount `<TxOverlay txState={txState} onDismiss={() => setTxState({status:'idle'})} />` always at root level — it self-hides when idle.